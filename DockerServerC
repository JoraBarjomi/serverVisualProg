FROM gcc:latest

RUN apt-get update && \
    apt-get install -y libzmq3-dev && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY server.c ./
RUN gcc server.c -o serverC -lzmq
EXPOSE 12345
CMD ["./serverC"]
